> Create by **fall** on 01 Dec 2025
> Recently revised in 01 Dec 2025

## 字符串

### int

```go
// converted to type int.

ParseInt(s, 10, 0)
fun ParseIntFn (){
  str1 := "1134"
  page, err1:= strconv.Atoi(str1)
  // strconv.Atoi 等价于 ParseInt(str1, 10, 0)
  page, err2:= strconv.ParseInt(

str1)
  
}
```

