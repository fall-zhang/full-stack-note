> Create by **fall** on 25 Feb 2024<br/>
> Recently revised in 25 Feb 2024

## postgresql

PostgreSQL 是一种关系型数据库管理系统 （RDBMS）关系实际上是*表*的数学术语。

虽然列在每行里的顺序是固定的， 但一定要记住 SQL 并不对行在表中的顺序做任何保证

RAID（Redundant Array of Independent Disks），提供单机冗余存储。
